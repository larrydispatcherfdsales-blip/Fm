name: 2. FMCSA Extractor (Single Batch)

on:
  workflow_dispatch:
    inputs:
      batch_index:
        description: 'The index of the batch to process (e.g., 0, 1, 2)'
        required: true
      batch_size:
        description: 'Number of MCs per batch'
        required: true
      concurrency:
        description: 'Parallel requests for the extractor script'
        required: true

jobs:
  run-extraction: # Job ka naam 'run-extraction' hai
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Create batch.txt for this specific job
        run: |
          # Yeh script ab khud mc_list.txt se apne hisse ki lines nikalegi
          START_LINE=$(( ${{ github.event.inputs.batch_index }} * ${{ github.event.inputs.batch_size }} + 1 ))
          BATCH_SIZE=${{ github.event.inputs.batch_size }}
          
          tail -n +$START_LINE mc_list.txt | head -n $BATCH_SIZE > batch.txt
          
          echo "✅ Created batch.txt for index ${{ github.event.inputs.batch_index }} with $(wc -l < batch.txt) lines."

      - name: Run the extractor script
        env:
          CONCURRENCY: ${{ github.event.inputs.concurrency }}
          BATCH_INDEX: ${{ github.event.inputs.batch_index }}
        run: |
          mkdir -p output
          # extractor.js ab hamesha batch.txt se data parhega
          node extractor.js

      - name: Upload the resulting CSV as an artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fmcsa-output-batch-${{ github.event.inputs.batch_index }}
          path: output/*.csv
          if-no-files-found: ignore
          retention-days: 1
